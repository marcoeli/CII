import 'package:cii/core/providers/session_state.dart';

// Alias old ContextProvider to new SessionProvider in Core
final contextProvider = sessionProvider;

// Typedefs might be needed if explicit types are used elsewhere?
// typedef ContextState = SessionState;
